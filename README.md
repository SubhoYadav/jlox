compile java code:
javac -d bin com/jlox/Jlox.java

Run the interpreter in CLI:
java -cp bin com.jlox.Jlox

Execute a file containing LOX code:
java -cp bin com.jlox.Jlox com/jlox/lox_scripts/<file-name>